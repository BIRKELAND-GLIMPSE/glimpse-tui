{"description":"For each supported trailing window, spent output profit ratio: spending value divided by creation-date value for outputs spent from an aggregate UTXO cohort. Values above one mean the outputs were spent in aggregate profit; values below one mean aggregate loss. Returns one when creation-date value is zero. Uses all UTXOs. Height-indexed matrix whose rows contain one value for every member of the matrix's column type.","indexes":["height"],"type":"[StoredF32; 3]"}